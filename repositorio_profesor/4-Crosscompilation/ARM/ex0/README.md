# Cross compilation example
Simple example of cross-compilation using arm-none-eabi-gcc. This is the ARM (arm) toolchain used to compile code for baremetal (none-) code using the eabi (eabi-) Application Binary Interface (ABI).
