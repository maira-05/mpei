# Multi-file build with variables and .PHONY                    

Create a Makefile that builds `main.c` and `util.c` into `app`. Use variables `CC`, `CFLAGS`, `LDFLAGS`. Provide all, run, and clean. Mark phony targets correctly.
   
